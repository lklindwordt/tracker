#= require jquery
#= require point
#= require helpers
#= require project
#= require loader

require ["jquery", "helpers", "project", "loader"], ($, Helper, Project, Loader) ->
  $ ->
    loader = new Loader Helper.scriptQuery("client.js")